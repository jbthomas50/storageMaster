<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.storagemaster.storagemaster">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="ExtraText">
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".NewItem"
            android:label="@string/title_activity_new_item"
            android:theme="@style/AppTheme.CustomTheme" />
        <activity
            android:name=".NewListActivity"
            android:label="@string/title_activity_new_list"
            android:theme="@style/AppTheme.CustomTheme" />
        <activity
            android:name=".SlideBarActivity"
            android:label="@string/title_activity_slider"
            android:theme="@style/AppTheme.CustomTheme" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name=".HintActivity"
            android:parentActivityName="com.example.storagemaster.storagemaster.MainActivity" >
            <!-- The meta-data element is needed for versions lower than 4.1 -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.storagemaster.storagemaster.MainActivity" />
        </activity>
    </application>

</manifest>